#include<bits/stdc++.h>
using namespace std;
int summm(int * arr , int i, int j)
{
	int summ=0;
	for(int ii=0;ii<9;ii++)
	{
		if(ii!=i && ii!=j)
		summ+=arr[ii];
	}
	return summ;
}
int main() {

	int n=9;
  int *arr = new int[9];
  int * arr2 = new int[7];
  for(int i=0;i<9;i++)
    cin >> arr[i];

	for(int i=0;i<9;i++)
	{
		for(int j=0;j<i;j++)
		{
			if(summm(arr,i,j)==100)
			{
				for(int b=0;b<9;b++)
				{
					if(b!=i && b!=j)
					{
						cout << arr[b] << endl;
					}
				}
			}
		}
	}
  

}
